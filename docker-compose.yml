version: "3.9"

services:
  docquest-backend:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: docquest-backend

    ports:
      - "8000:8000"

    env_file:
      - .env

    restart: unless-stopped